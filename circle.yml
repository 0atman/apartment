machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull jagregory/pandoc

test:
  override:
    - make
    - git config --global user.email "tristram@oaten.name"
    - git config --global user.name "circlebot"
    - git add index.html
    - git commit -m 'updated index'
    - git push
